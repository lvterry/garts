generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Artwork {
  id        String   @id @default(uuid())
  keyword   String
  mood      String
  artData   String   // JSON stringified
  createdAt DateTime @default(now())
}
